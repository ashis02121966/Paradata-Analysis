<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }
        .watermark {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-45deg);
            font-size: 120px;
            color: rgba(0, 0, 0, 0.05);
            z-index: -1;
            font-weight: bold;
            pointer-events: none;
        }
        .container {
            max-width: 900px;
            margin: 0 auto;
            background: white;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            position: relative;
        }
        .header {
            text-align: center;
            margin-bottom: 40px;
            border-bottom: 3px solid #667eea;
            padding-bottom: 20px;
        }
        .header h1 {
            color: #333;
            margin: 0;
            font-size: 32px;
            font-weight: bold;
        }
        .header .subtitle {
            color: #667eea;
            margin: 10px 0 0 0;
            font-size: 16px;
            font-weight: 500;
        }
        .meta-info {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 20px;
            margin-bottom: 30px;
        }
        .meta-card {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 20px;
            border-radius: 8px;
            text-align: center;
        }
        .meta-card h3 {
            margin: 0 0 5px 0;
            font-size: 24px;
            font-weight: bold;
        }
        .meta-card p {
            margin: 0;
            opacity: 0.9;
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .department-summary {
            margin-bottom: 30px;
            padding: 20px;
            background: #f8f9fa;
            border-radius: 8px;
            border-left: 4px solid #667eea;
        }
        .department-summary h3 {
            margin: 0 0 15px 0;
            color: #333;
            font-size: 18px;
        }
        .dept-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 15px;
        }
        .dept-item {
            background: white;
            padding: 15px;
            border-radius: 5px;
            text-align: center;
            border: 1px solid #e0e0e0;
        }
        .dept-item strong {
            display: block;
            color: #667eea;
            font-size: 18px;
            margin-bottom: 5px;
        }
        .dept-item span {
            color: #666;
            font-size: 12px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            font-size: 11px;
        }
        th, td {
            padding: 12px 8px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        th {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            font-weight: bold;
            text-transform: uppercase;
            font-size: 10px;
            letter-spacing: 0.5px;
        }
        tr:nth-child(even) {
            background-color: #f8f9fa;
        }
        tr:hover {
            background-color: #e3f2fd;
        }
        .salary-high {
            color: #28a745;
            font-weight: bold;
        }
        .salary-medium {
            color: #ffc107;
            font-weight: bold;
        }
        .salary-low {
            color: #dc3545;
            font-weight: bold;
        }
        .footer {
            margin-top: 40px;
            text-align: center;
            color: #666;
            font-size: 12px;
            border-top: 1px solid #ddd;
            padding-top: 20px;
        }
        .footer .company-info {
            margin-bottom: 10px;
            font-weight: bold;
            color: #333;
        }
    </style>
</head>
<body>
    <div class="watermark">EMPLOYEE REPORT</div>
    <div class="container">
        <div class="header">
            <h1>{{title}}</h1>
            <p class="subtitle">Comprehensive Employee Directory & Analytics</p>
            {{#if description}}
            <p style="color: #666; margin-top: 15px; font-style: italic;">{{description}}</p>
            {{/if}}
        </div>
        
        <div class="meta-info">
            <div class="meta-card">
                <h3>{{totalUsers}}</h3>
                <p>Total Employees</p>
            </div>
            <div class="meta-card">
                <h3>{{generatedDate}}</h3>
                <p>Report Date</p>
            </div>
            <div class="meta-card">
                <h3>{{generatedTime}}</h3>
                <p>Generated Time</p>
            </div>
            <div class="meta-card">
                <h3>${{avgSalary}}</h3>
                <p>Avg Salary</p>
            </div>
        </div>

        {{#if departmentStats}}
        <div class="department-summary">
            <h3>Department Distribution</h3>
            <div class="dept-grid">
                {{#each departmentStats}}
                <div class="dept-item">
                    <strong>{{count}}</strong>
                    <span>{{department}}</span>
                </div>
                {{/each}}
            </div>
        </div>
        {{/if}}

        {{#if users}}
        <table>
            <thead>
                <tr>
                    <th>Employee ID</th>
                    <th>Full Name</th>
                    <th>Email Address</th>
                    <th>Phone Number</th>
                    <th>Department</th>
                    <th>Position</th>
                    <th>Annual Salary</th>
                    <th>Hire Date</th>
                </tr>
            </thead>
            <tbody>
                {{#each users}}
                <tr>
                    <td>EMP-{{id}}</td>
                    <td>{{name}}</td>
                    <td>{{email}}</td>
                    <td>{{phone}}</td>
                    <td>{{department}}</td>
                    <td>{{position}}</td>
                    <td class="{{#if (gt salary 80000)}}salary-high{{else}}{{#if (gt salary 60000)}}salary-medium{{else}}salary-low{{/if}}{{/if}}">
                        ${{salary}}
                    </td>
                    <td>{{hire_date}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
        {{/if}}

        <div class="footer">
            <div class="company-info">ABC Corporation - Human Resources Department</div>
            <p>This report contains confidential employee information and should be handled according to company privacy policies.</p>
            <p>Generated automatically by the Employee Management System | © 2024 All rights reserved.</p>
        </div>
    </div>
</body>
</html>